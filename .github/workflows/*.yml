- name: Install Dependencies
  run: |
    python -m pip install --upgrade pip
    pip install --retries 10 -r requirements.txt

    - name: Install Dependencies
  run: |
    python -m pip install --upgrade pip urllib3
    pip install -r requirements.txt
